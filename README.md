* Each file in this repo is a shell script

* Place files in the `~/bin` directory

* Set file permissions to be executable

```
chmod +x filename
```

* Use the terminal to call from anywhere

```
filename
```
